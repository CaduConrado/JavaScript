let carro = {
  rodas: 4,
  portas: 4,
  arCondicionado: true,
  motor: 3.0,
};

const {
  rodas: vRodas,
  portas: vPortas,
  arCondicionado: vArCondicionado,
  motor: vMotor,
} = carro;

console.log(vMotor);
console.log(vArCondicionado);
